extends layout

block content
  .login.m-sm-5
    .row.justify-content-center
      .col-sm-3

      .login-box.box.col-sm-6.p-3.p-sm-4.align-items-center
        h1.fw-4.mt-3.text-center= title
        
        .login-form.p-3
          form(action="/login" method="POST")
            .form-group.row.mb-3
              label.col-12.text-start.col-form-label(for="email") Email: 
              .col-12
                input#login-email.form-control(type="text" name="email" placeholder="email" value=email required)
                p.mt-2.err-msg(style="color: red") #{emailError}

            .form-group.row.mb-3
              label.col-12.text-start.col-form-label(for="password") Password:
              .col-12.input-group
                input#password.form-control(type="password" name="password" placeholder="password" required)
                .input-group-append
                    span#togglePassword(class="input-group-text") 
                      i(class="fa fa-eye")
              p.mt-2.mx-3.mb-3.err-msg(style="color: red") #{passwordError}

            .form-group.row.mt-sm-4.mt-3
              .col-12.text-center
                button.btn.custom-btn.col-12(type="submit") LOGIN

            .form-group.row.mb-1
              .col-12.text-center
                p Don't have an account? 
                  a(href='/registration') Register

      .col-sm-3
